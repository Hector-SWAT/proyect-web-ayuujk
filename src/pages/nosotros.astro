---
// src/pages/nosotros.astro
import Layout from "../layouts/Layout.astro";
import MobileMenu from "../components/MobileMenu.astro";
---

<!-- /* =========================================================
   Proyecto desarrollado con Astro + TailwindCSS + Node.js
   Autores: Hector-SWAT & Luis
   Año: 2025
   DIMENSIONS 430x932 - MOBILE OPTIMIZED
========================================================= */ -->
<Layout title="AYUUJK - Servicios de Taxi en Los Angeles">
    <div
        class="min-h-screen bg-cover bg-center bg-no-repeat relative"
        style="background-image: url('/services-icon/Fondo-services.png');"
    >
        <!-- Elemento decorativo amarillo latera FUNCIONA -->
        <div class="absolute right-0 top-0 w-24 h-full">
            <button
                class="w-full h-full hover:scale-105 transition-transform duration-300"
            >
                <img
                    src="/services-icon/slide-services.png"
                    alt="Barra lateral"
                    class="w-full h-full object-cover"
                />
            </button>
        </div>

        <!-- Contenido principal -->
        <main class="relative z-10 h-full flex flex-col">
            <div class="flex items-center ml-6 mt-12">
                <a href="index.astro" class="flex items-center">
                    <img
                        src="/logo.png"
                        alt="AYUUJK Logo"
                        class="w-16 h-16 object-contain"
                    />
                </a>
            </div>

            <!-- Sección NOSOTROS -->
            <div class="px-8 pb-12 text-center space-y-6">
                <h2
                    class="text-4xl font-bold text-yellow-400 mb-6 tracking-wider"
                >
                    NOSOTROS
                </h2>

                <!-- Texto principal -->
                <p class="text-lg text-gray-200 leading-relaxed">
                    <span
                        class="block font-extrabold text-2xl text-yellow-400 mb-2"
                        >YUUJK TAXI</span
                    >
                    Tu mejor opción en <span class="font-semibold"
                        >Los Ángeles</span
                    >.
                </p>

                <p class="text-gray-300 leading-relaxed">
                    Somos un grupo de taxistas mexicanos en constante
                    crecimiento, ofreciendo traslados rápidos, económicos y
                    seguros en Los Ángeles, CA.
                </p>

                <!-- Lista de ventajas -->
                <ul class="text-left inline-block text-gray-200 space-y-2">
                    <li>
                        🚕 <span class="font-semibold"
                            >Confianza y puntualidad</span
                        > en cada viaje.
                    </li>
                    <li>
                        📦 <span class="font-semibold"
                            >Servicio de delivery y mudanzas</span
                        >, para tu comodidad.
                    </li>
                    <li>
                        👥 <span class="font-semibold">Atención cercana</span> y
                        siempre disponible.
                    </li>
                </ul>

                <!-- Contacto -->
                <div class="space-y-1 mt-4">
                    <p class="text-gray-200">
                        <span class="font-semibold">📱 Llámanos:</span> 323-374-8922
                    </p>
                    <p class="text-gray-200">
                        <span class="font-semibold">📱 WhatsApp:</span> 323-901-0216
                    </p>
                </div>

                <!-- Frase final -->
                <p class="italic text-yellow-300 font-semibold text-lg mt-6">
                    “Más que un traslado, somos tu aliado en el camino”.
                </p>
            </div>

            <!-- Indicador inferior -->
            <div class="flex justify-center pb-4">
                <div class="w-32 h-1 bg-white/30 rounded-full"></div>
            </div>
        </main>
    </div>
</Layout>

<style is:global>
    :root {
        --font-oswald: "Oswald", sans-serif;
        --font-poppins: "Poppins", sans-serif;
    }

    body {
        font-family: var(--font-poppins);
        background-color: #111827;
        color: white;
        margin: 0;
        padding: 0;
        overflow-x: hidden;
    }

    h1,
    h2 {
        font-family: var(--font-oswald);
    }

    h1 {
        text-shadow: 0 4px 20px rgba(0, 0, 0, 0.8);
        letter-spacing: 0.1em;
    }

    h2 {
        text-shadow: 0 2px 10px rgba(255, 193, 7, 0.5);
    }

    /* Animación flotante para el taxi */
    .floating-taxi {
        animation: float 6s ease-in-out infinite;
        filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.3));
    }

    @keyframes float {
        0%,
        100% {
            transform: translateY(0px) rotate(0deg);
        }
        25% {
            transform: translateY(-10px) rotate(1deg);
        }
        50% {
            transform: translateY(-5px) rotate(0deg);
        }
        75% {
            transform: translateY(-15px) rotate(-1deg);
        }
    }

    /* Efectos de iluminación para las tarjetas */
    .hover\:bg-orange-200\/20:hover {
        position: relative;
        overflow: hidden;
    }

    .hover\:bg-orange-200\/20:hover::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            90deg,
            transparent,
            rgba(255, 255, 255, 0.1),
            transparent
        );
        transition: left 0.5s;
    }

    .hover\:bg-orange-200\/20:hover::before {
        left: 100%;
    }

    /* Estilos responsivos */
    @media (max-width: 430px) {
        .text-4xl {
            font-size: 2rem;
        }
    }
</style>
